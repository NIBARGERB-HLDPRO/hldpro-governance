# Ephemeral / large
history.jsonl
sessions/
cache/
paste-cache/
file-history/
session-env/
plans/
!docs/plans/
!docs/plans/*.json
!docs/plans/*.md
downloads/
backups/

# Sensitive
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Per-project memory (conversation-specific, managed by auto-memory)
projects/

# Plugins (installed separately)
plugins/

# Misc ephemeral
shell-snapshots/
stats-cache.json
tasks/
image-cache/

# graphify (large binary outputs)
graphify-out/graph.html
graphify-out/cache/
graphify-out/.graphify_*.json

# raw/ large media files (append-only text files are tracked)
raw/**/*.mp3
raw/**/*.wav

# Claude Code local tooling state (worktrees, local settings)
.claude/

# OS
.DS_Store

# Operator secret vault — NEVER COMMIT
.env
.env.shared
.env.local
.env.*.local
