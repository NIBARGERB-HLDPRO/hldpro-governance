# Alternate-Family Specialist Review: Issue #623 Implementation Slice

**Reviewer:** Claude Opus 4.6 (alternate-family specialist)
**Review scope:** Bounded implementation slice for issue #623 — mutation-time pre-tool fail-closed hardening
**Date:** 2026-04-30
**Status:** `accepted`

---

## Findings

**No blocking issues found.**

### Confirmed Correct Behavior

1. **Scope boundedness (Q1):** The modified file set matches the declared execution scope exactly: `.claude/settings.json`, `hooks/code-write-gate.sh`, `hooks/schema-guard.sh`, `scripts/overlord/check_plan_preflight.py`, three focused test files, three governance doc co-staging files (`PROGRESS.md`, `FEATURE_REGISTRY.md`, `FAIL_FAST_LOG.md`), and issue-local artifacts under `raw/`, `docs/plans/`, and `docs/codex-reviews/`. No modifications to `hooks/pre-session-context.sh`, `check_execution_environment.py`, `assert_execution_scope.py`, `backlog-check.sh`, `governance-check.sh`, CI workflows, or `delegation_gate.py`.

2. **Edit/MultiEdit/Bash fail-closed enforcement (Q2):** `.claude/settings.json` now wires `PreToolUse` matchers for `Edit` and `MultiEdit` (in addition to existing `Write`) to `code-write-gate.sh`. The hook enforces the full governance chain — plan preflight via `check_plan_preflight.py` (now using canonical `docs/plans/*structured-agent-cycle-plan.json`), structured plan validator, and execution-scope validator — before allowing any mutation. `schema-guard.sh` enforces the same plan preflight for Bash commands and hard-blocks detected file writes. Bash mutation detection in `check_plan_preflight.py` now covers `sed -i`, `perl -pi`, `cp`, `mv`, `install`, `touch`, `truncate`, `dd of=`, redirects, `tee`, heredocs, and Python `open()`/`write_text()` — with indeterminate targets (for example, `sed -i` without a filename) also hard-blocked. Read-only pipelines (`grep`, `rg`, `git status`, `find`, `awk`, `jq`, `sed -n`, `perl -ne`) are confirmed allowed by tests.

3. **Fail-closed on error conditions (Q3):** Both hooks now hard-block (exit 2) on every named failure mode:
   - `code-write-gate.sh`: malformed payload, missing `file_path`, missing validator, missing plan preflight helper, empty preflight decision, unknown preflight decision, validator non-zero exit, unresolvable repo root.
   - `schema-guard.sh`: malformed input payload, missing helper, empty decision, unknown decision, validator failure.
   - All are confirmed by dedicated test cases in `test_delegation_hook.py` and `test_schema_guard_hook.py`.

4. **Boundary preservation (Q4):** The PDCAR, structured plan, execution scope, and handoff all explicitly list `#617`, `#621`, `#607`, `#612`, and `#614` as external boundaries. The execution scope's `forbidden_roots` and `active_parallel_roots` enumerate the sibling worktree paths. The `material_deviation_rules` define six specific violation conditions. The actual implementation diff stays entirely within the declared owned surfaces.

5. **Proof specificity without overclaim (Q5):** The validation artifact contains a commands table and a per-surface proof matrix with exact transcripts. The PDCAR Review section explicitly states that issue `#623` may close only the bounded source-repo local mutation-time pre-tool hardening slice and does not close `#607`, `#612`, `#614`, or the broader `#615` program. The feature registry keeps `GOV-014` at `IN_PROGRESS`.

## Open Questions

None blocking.

## Validation Notes

1. **Implementation gates replayed cleanly after artifact normalization:** `local-ci-gate`, `validate_closeout.py`, and `git diff --check` all passed against the final issue-scoped artifacts.

2. **Empty-input graceful degradation remains explicit, not hidden:** both hooks still exit `0` on empty stdin. That is consistent with the existing hook-runner contract and is not a regression in this slice.

3. **Integration tests remain coupled to live governance state:** the owned-surface allow tests depend on valid issue-623 plan/scope evidence in the live worktree, which is expected for governance-path integration tests.

## Summary

The implementation slice is well-bounded, correct, and properly scoped to the local mutation-time pre-tool path. The fail-closed behavior is comprehensive across all named error conditions. `Edit`, `MultiEdit`, and expanded Bash mutation verbs are now enforced through the canonical plan-evidence and governance-surface chain. Sibling-lane boundaries (`#617`, `#621`, `#607`, `#612`, `#614`) are preserved with no surface violations. The proof chain is specific to `AC1-AC5` without overclaiming broader `#615` closure.

**Review decision: accepted — no blocking findings.** Residual items: complete the remaining local gates and closeout validation after final artifact normalization.
