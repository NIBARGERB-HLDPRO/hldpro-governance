# Alternate-Family Specialist Review — Issue #617

**Reviewer:** Claude Opus 4.6 (alternate-family)
**Review scope:** Bounded startup/helper implementation for fail-closed lane-state surfacing
**Date:** 2026-04-30

## Findings

**No blocking findings.**

The implementation is narrowly bounded to the startup/helper surfacing path. Every review question is satisfied:

### Q1: Narrow scope boundedness

The write set is strictly limited to the five implementation files and their associated governance artifacts. No broader pre-hook write-blocking logic, execution-scope schema changes, or consumer drift gate code is present. The `allowed_write_paths` in the implementation scope (`raw/execution-scopes/2026-04-30-issue-617-prehook-startup-failclosed-implementation.json:5-20`) is appropriately constrained. The `forbidden_roots` list (lines 22-28) explicitly fences off all sibling worktrees.

### Q2: Deterministic PASS/BLOCKED behavior

`check_execution_environment.py` emits exactly `PASS startup execution context` or `BLOCKED startup execution context`. The selection algorithm in `_select_scope` is deterministic and covers all disambiguation cases: no match, unique implementation match, multiple implementation matches, unique planning match, and multiple planning matches. The hook (`pre-session-context.sh`) always exits 0, which is correct because non-zero exits in `UserPromptSubmit` hooks suppress output. The fail-closed posture is informational surfacing, not write-time enforcement, which matches the PDCAR's stated scope.

### Q3: Sibling-lane preservation

`#607`, `#612`, and `#614` are preserved as explicit external boundaries in the structured plan, implementation handoff, material deviation rules, and the PDCAR Adjust section. No surfaces owned by those issues are modified.

### Q4: Proof artifact sufficiency

The validation artifact records the local passing commands for the bounded slice. Two startup transcripts are included: a PASS transcript showing resolved scope path, execution mode, next role, and required specialists; and a BLOCKED transcript showing the expected failure on a non-matching branch. The focused tests cover four distinct scenarios: unique implementation selection, duplicate implementation blocking, duplicate planning blocking, and branch mismatch blocking. The integration test in `scripts/test_session_bootstrap_contract.py` runs the full hook pipeline end-to-end from a non-root subdirectory session.

## Open Questions

1. **Planning-only scope selection in startup preflight:** If only a `planning_only` scope matches, `_select_scope` returns it and `_startup_summary` runs `check_scope`, which may PASS. Whether that should always emit an additional WARN that no implementation-capable scope was found is a follow-up design choice, not a defect in this slice.

2. **Private API coupling:** `_discover_scope_candidates` calls underscore-prefixed helpers from `assert_execution_scope.py`. That is acceptable for co-located governance modules, but worth refactoring later if these helpers become shared public surfaces.

## Validation Gaps

None identified for this slice. CI-level enforcement for these tests belongs to `#612` and remains correctly out of scope here.

## Summary

The implementation satisfies all four review questions. It remains narrowly bounded to startup/helper surfacing, emits deterministic PASS/BLOCKED output, explicitly preserves sibling-lane ownership, and provides sufficient proof artifacts for this slice. No material deviation from the PDCAR or structured plan was found.

**Verdict: ACCEPTED — no blocking findings. Residual risk is limited to the planning-only scope selection design question, which is a follow-up item, not a defect.**
