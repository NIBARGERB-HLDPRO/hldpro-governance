I now have a complete picture. Here is my review.

---

# Alternate-Family Specialist Review — Issue #632 Implementation Slice

**Reviewer**: Claude Opus 4.6 (anthropic)
**Date**: 2026-04-30
**Scope**: Implementation slice for issue #632 — planning-authority enforcement tightening in `assert_execution_scope.py`

## Findings

### F1 — Core enforcement is correct and minimal (non-blocking, positive)

The implementation adds exactly three things to `assert_execution_scope.py`:
- `PLANNING_ONLY_PREFIXES` / `PLANNING_ONLY_FILES` constants (lines 14-27)
- `_is_planning_only_path()` predicate (lines 410-411)
- A planning-only enforcement check in `check_scope()` (lines 618-624)

When `execution_mode == "planning_only"`, any changed path that is NOT in the planning-only set produces a hard `FAIL`, even if the path appears in `allowed_write_paths`. This is the correct fail-closed design: a planning scope can declare future implementation paths in its allowlist (for the later `implementation_ready` scope to use) without being able to actually authorize writes to them.

### F2 — Key test proves the boundary (non-blocking, positive)

`test_planning_only_diff_in_allowed_implementation_path_fails` (test file line 193) directly proves the stated AC2: a `planning_only` scope with `scripts/overlord/` in `allowed_write_paths` still rejects a diff to `scripts/overlord/assert_execution_scope.py`. This is the exact "blocked-before-authority" proof.

### F3 — Existing tests updated to align with new invariant (non-blocking, positive)

Tests that previously used `allowed.txt` / `allowed-dir/nested.txt` with the default `planning_only` mode were correctly migrated to use planning-only paths (`docs/plans/`, `raw/validation/`). Without this update, the new enforcement would have broken them — the migration is necessary and well-done.

### F4 — governance-check.yml and local-ci-gate profile not directly modified (non-blocking, follow-up)

The structured plan Sprint 2 and AC1 list `.github/workflows/governance-check.yml` and `tools/local-ci-gate/profiles/hldpro-governance.yml` as owned implementation surfaces, but git status shows neither file was modified. The enforcement tightening in `assert_execution_scope.py` itself may be sufficient if those callers already invoke the script — but the validation artifact should confirm that the gate invocation path is already wired, or the closeout should note this as a future wiring step under a follow-up issue.

### F5 — Closeout validator replay still pending (non-blocking, carry-forward)

The validation artifact records `validate_closeout.py` as `PENDING FOLLOW-UP`. This was noted in the prior planning-packet review (F2 of `docs/codex-reviews/2026-04-30-issue-632-claude.md`). It should be replayed and written back before the PR merge gate.

### F6 — `docs/PROGRESS.md` is planning-only but `docs/FAIL_FAST_LOG.md` and `docs/FEATURE_REGISTRY.md` are not (non-blocking, design confirmation)

`PLANNING_ONLY_FILES` includes `OVERLORD_BACKLOG.md` and `docs/PROGRESS.md` but excludes `docs/FAIL_FAST_LOG.md` and `docs/FEATURE_REGISTRY.md`. This means the latter two require an `implementation_ready` scope to modify — consistent with the fact that both appear only in the implementation execution scope, not the planning scope. Correct by design.

## Open Questions

1. **Gate wiring**: Is `assert_execution_scope.py` already invoked by `governance-check.yml` and the local-ci-gate profile, making the tightening automatically effective? If not, the callers need explicit wiring and that should be tracked.

## Validation Gaps

1. `validate_closeout.py` replay is still pending — must complete before merge.
2. Confirm the governance-check workflow and local-ci-gate profile already invoke `assert_execution_scope.py` with the `--scope` flag that triggers the new `planning_only` enforcement path, or track the wiring gap.

## Summary

The implementation is clean, minimal, and correctly bounded. The core invariant — `planning_only` scopes cannot authorize implementation-shaped diffs even when those paths appear in `allowed_write_paths` — is proven by both the code (`check_scope` lines 618-624) and the test suite (35/35 pass, including the critical `test_planning_only_diff_in_allowed_implementation_path_fails`).

- No scope bleed into hooks, fallback-propagation, startup/pretool, or sim-verifier surfaces.
- #612, #614, #615, and #631 remain explicit external boundaries across all artifacts.
- `implementation_ready` handoff path is preserved with full handoff-evidence validation.
- No confirmed defects found.
- Residual risks are limited to gate-wiring confirmation and closeout-validator replay.

**Verdict: `accepted_with_followup`**

Follow-up items (non-blocking for implementation merge, blocking for closeout):
1. Replay `validate_closeout.py` and write results into the validation artifact.
2. Confirm or wire the governance-check.yml / local-ci-gate invocation of the tightened `assert_execution_scope.py`.
