All packet artifacts read. Producing the alternate-family review verdict.

---

# Issue #587 Alternate-Family Review (Implementation-Ready Gate)

Date: 2026-04-29
Reviewer: Claude Opus 4.6 (alternate-family)
Issue: https://github.com/NIBARGERB-HLDPRO/hldpro-governance/issues/587
Branch: `issue-587-rollout-blockers`
Execution mode: `planning_only` (reviewing for implementation-ready promotion)

## Verdict

**`accepted`** — Implementation-ready promotion should proceed.

No missing source blocker was identified. The blocker set (AC1–AC10) is the minimum sufficient set to unblock downstream rollout.

## Findings

### Blocking: none.

### Medium (non-blocking, should resolve during implementation)

1. **Handoff JSON missing required specialist fields.** The updated `package-handoff.schema.json` (lines 21–24) now requires `specialist_agent`, `packet_transport`, `packet_output_ref`, and `availability_ref`. The current handoff artifact (`raw/handoffs/2026-04-29-issue-587-rollout-blockers.json`) omits these keys entirely. JSON Schema `required` demands the key be present even when the value is `null`. The validator reports PASS, so either the validator is lenient on absent-but-nullable fields, or it was run before the schema was tightened. **Action:** During implementation, either populate these fields or add them as explicit `null` keys. Verify the validator enforces presence, not just non-null.

### Minor (non-blocking)

2. **Review artifact filename mismatch (carried forward).** The cross-review artifact (line 19) references `docs/codex-reviews/2026-04-29-claude.md`, while the structured plan's `alternate_model_review.evidence` and the handoff's `review_artifact_refs` point to `docs/codex-reviews/2026-04-29-issue-587-claude.md`. Git status shows both paths exist on the branch. Reconcile to one canonical filename during implementation.

3. **Validation artifact records `implementation_ready` while this review scope is `planning_only`.** The validation file header says `execution_mode: implementation_ready`, suggesting it was updated after the execution mode was promoted from `planning_only`. This is a temporal ordering artifact, not a logic defect, but the validation artifact should note when it was re-run post-promotion so the audit trail is unambiguous.

4. **Specialist review array under-populated.** The structured plan's `specialist_reviews` array has one entry (gpt-5.4-mini scope-confirmation). The review packet cites three additional specialist verdicts (validator/handoff, session-contract, rollout-readiness) that appear only in packet prose. These should be reconciled into the `specialist_reviews` array during implementation so validators can bind all specialist evidence programmatically.

## Open Questions

None that block implementation-ready promotion.

## Validation Gaps

- **For planning scope:** None. All four validation commands pass per the validation artifact.
- **For implementation scope:** The handoff schema field-presence gap (finding #1) should be gated by the implementation sprint's own QA pass. AC8 and AC9 both name `validate_handoff_package.py` as verification refs, so the implementation sprint is structurally required to prove these fields validate.

## Routing Findings

- Routing is explicit as bidirectional `Codex <> Claude` pinned-agent dispatch. The `dispatch_contract` in both the structured plan and handoff artifact correctly declares `primary_session_family: "openai"`, routes `alternate_model_review` to `anthropic`, and pins the governed wrapper path (`scripts/codex-review.sh`, wrapper id `claude`).
- Claude remains alternate-family review only in this lane. This review artifact is the sole Claude-side contribution; bounded work and QA remain on the Codex-side worker/subagent path.
- The `fallback_policy` correctly constrains fallback to same-family-only with `const: true` enforcement at the schema level, preventing cross-family role absorption structurally.
- The fallback ladder (`gpt-5.3-codex-spark` -> `gpt-5.4`) is upward-quality, consistent with AC10's requirement that fallback "move only upward through the approved quality ladder."
- Neither side may absorb the other side's pinned role. The schema's `dispatchContract` definition in both `package-handoff.schema.json` and `structured-agent-cycle-plan.schema.json` enforces this at the structural level.

## Summary

The issue #587 blocker set is correctly bounded, source-owned, and sufficient. The ten acceptance criteria cover the full surface: schema/validator lifecycle alignment (AC1), consumer contract coverage (AC2–AC3), canonical packet transport (AC4), bidirectional routing hard-gate (AC5), end-of-change auditor gate (AC6), tracked specialist lanes (AC7–AC8), machine-readable dispatch ownership (AC9), and pinned-specialist fallback policy (AC10). The scope boundary explicitly excludes consumer-repo mutations and reopening merged fixes. The material-deviation rules are enforceable.

No additional source blocker was identified. The four non-blocking findings should be absorbed into the implementation sprint but do not warrant blocking promotion.

**Implementation-ready promotion should proceed.**

---

**Residual risk:** The handoff artifact's absent specialist fields (finding #1) could cause a validator regression if the schema enforcement is tightened before the fields are populated. This is bounded by the implementation sprint's own AC8/AC9 verification pass.
