# Claude Review — Issue #619

Date: 2026-04-30
Issue: `#619`
Phase: `planning_only`
Reviewer: `claude-opus-4-6` (Anthropic alternate-family)
Status: `PASS — no blocking findings`

## Findings

No blocking defects found. The planning-only packet is well-constructed and internally consistent across all five artifacts.

### Q1: Scope bounded to local mutation-time pre-tool surfaces only?

**PASS.** The PDCAR, structured plan `scope_boundary`, execution scope `allowed_write_paths`, and handoff ACs all converge on exactly four owned implementation surfaces:

- `.claude/settings.json`
- `hooks/code-write-gate.sh`
- `hooks/schema-guard.sh`
- `scripts/overlord/check_plan_preflight.py`

Plus focused tests and issue-local artifacts. The Adjust section (PDCAR lines 103-119) defines four explicit stop-and-reroute triggers for scope drift. The execution scope's `allowed_write_paths` restricts the bootstrap phase to planning artifacts only. The `material_deviation_rules` in the structured plan enumerate five specific overreach scenarios that would invalidate the lane.

### Q2: Preserves #617 as closed; keeps #607, #612, #614 as separate lanes?

**PASS.** All five artifacts treat these consistently:

- **#617**: Called "already-closed startup/helper child" in the PDCAR (line 19), the structured plan `scope_boundary` (line 14), and the handoff `blocked_on` list. The execution scope lists the #617 worktree as a `forbidden_root`.
- **#607**: Kept as "planning-authority dependency" throughout. PDCAR Check (line 63) explicitly requires the lane keep #607 as dependency rather than absorbing its contract.
- **#612**: Kept as "degraded-fallback schema and CI enforcement lane." PDCAR Adjust (line 109) explicitly stops and reroutes if the lane touches `assert_execution_scope.py`, fallback-log schema surfaces, or CI workflow enforcement. Execution scope lists the #612 worktree as `forbidden_root`.
- **#614**: Kept as "downstream consumer verifier or drift-gate lane" in the PDCAR, structured plan `out_of_scope`, and handoff `blocked_on`.

AC2 in the handoff directly requires this preservation.

### Q3: Implementation blocked until planning validation + alternate-family review?

**PASS.** Three independent signals enforce this gate:

1. `execution_mode: "planning_only"` in both the structured plan and execution scope.
2. `alternate_model_review.required: true`, now recorded as `status: "accepted"` in the structured plan after this review completed.
3. AC3 in the handoff: "No mutation-path implementation may begin until the issue-local planning packet validates and alternate-family review is recorded."

The `execution_handoff.next_execution_step` confirms: "Validate the planning packet, collect alternate-family review, and only then decide whether to promote issue #619 into an implementation-ready execution scope."

Material deviation rule 4 explicitly flags any pre-validation implementation as a deviation.

### Q4: ACs and proof obligations specific enough for exact blocked/allowed evidence?

**PASS.** AC4 in the handoff is highly prescriptive. It requires a per-surface proof matrix with:

- Edit or MultiEdit blocked case
- Named Bash mutation verbs blocked case
- Removed fail-open behavior blocked case
- One compliant governance-surface allowed case
- One read-only or out-of-scope allowed case

Each requiring "exact command, tool path, exit code, and block or allow result."

The PDCAR Check section (lines 79-98) adds further specificity: blocked transcripts must include the exact command and block message, bounded-write proof must show the touched-file list stays inside owned surfaces, and explicit no-touch proof is required for `hooks/pre-session-context.sh`, `scripts/overlord/check_execution_environment.py`, `scripts/overlord/assert_execution_scope.py`, CI workflows, and other sibling-lane surfaces.

These obligations are local-mutation-path-specific and do not drift into CI/schema or broader hook rewrites.

## Open Questions

1. **Specialist review "mixed" status**: All three specialist reviews in the structured plan carry `status: "mixed"` rather than `"pass"`. The summaries indicate findings were incorporated (e.g., the critical audit specialist drove the per-surface proof matrix requirement). A brief resolution note per specialist would improve auditability but is not blocking.

2. **#614 worktree absence from forbidden_roots**: The execution scope lists forbidden roots for #615, #617, and #612 worktrees but not #614. This appears correct — #614 likely has no active worktree yet — but the lane should add it if one is created before #619 implementation begins.

## Validation Gaps

None identified. The validation artifact records all six validation commands as PASS. The packet shape is internally consistent: PDCAR, structured plan, execution scope, handoff, and validation artifact all tell the same story with no contradictions.

## Summary

The issue #619 planning-only packet passes alternate-family review with no blocking findings. The scope is correctly narrowed to local mutation-time pre-tool hardening on four named surfaces. Sibling lanes (#617, #607, #612, #614) are consistently preserved as external boundaries across all artifacts. Implementation is gated behind planning validation and this review. The future proof obligations are concrete enough to require exact transcript-level evidence rather than prose attestation. Residual risk is low — the two open questions are informational only.

This review satisfies the `alternate_model_review` obligation in the structured plan and unblocks promotion to implementation-ready execution scope at the orchestrator's discretion.
